import { EditorNodeType } from '@/lib/types'
import React from 'react'

type Props = { nodes: EditorNodeType[]}

const EditorCanvasSidebar = (props: Props) => {
  return (
    <div>EditorCanvasSidebar</div>
  )
}

export default EditorCanvasSidebar